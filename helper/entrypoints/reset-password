#!/bin/sh
# Polkit action com.local.usermanager.reset-password maps its exec.path here.
# The operation name is a fixed literal, never taken from argv/input.
exec /usr/lib/ubuntu-user-manager/user-manager-helper reset-password
